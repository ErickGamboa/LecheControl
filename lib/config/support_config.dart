/// Contacto de soporte público, mostrado en pantallas de cuenta.
class SupportConfig {
  SupportConfig._();

  static const String email = 'soporte@lechecontrol.com';

  /// Teléfono o WhatsApp opcional. Si es null, la UI solo muestra el correo.
  static const String? phone = null;
}
